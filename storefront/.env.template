# Medusa Backend Connection
NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Default region country code (fallback when geo-detection fails)
NEXT_PUBLIC_DEFAULT_REGION=us

# Base URL of this storefront (used for image domains, OG tags, etc.)
# NEXT_PUBLIC_BASE_URL=http://localhost:8000
